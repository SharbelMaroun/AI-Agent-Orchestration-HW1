# Fourier Neural Decoder

This directory holds the Python package. The canonical project documentation — installation, usage, configuration, training, and the project report — lives in the **root README** at [`../README.md`](../README.md).

This file exists only to satisfy the package metadata (`pyproject.toml::readme`).
